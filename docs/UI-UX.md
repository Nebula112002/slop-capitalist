The brief lives in [`docs/OPUS.md`](OPUS.md).

How the UI is actually built — tokens, component anatomy, the render/patch
split, the fold budget, a11y checklist, recipes and the traps that cost time —
is in [`docs/DESIGN-SYSTEM.md`](DESIGN-SYSTEM.md). Read that before changing
chrome.
