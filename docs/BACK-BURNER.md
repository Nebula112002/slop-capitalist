# Back-burner

**Superseded.** All ideas from this file (cycle speed, theme, flavor, buy presets, tap layer, sticky chrome, prestige spam) plus the later menus / outside-vs-inside view live in:

**[`docs/MASTER-PLAN.md`](MASTER-PLAN.md)**

That file is the single source of truth. Status: back burner — do not implement from either document until Caleb says go.

This stub exists so old links and the `cursor/back-burner-plan-be55` branch are not orphaned.
