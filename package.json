{
  "name": "slop-capitalist",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Idle tycoon about farming the algorithm. AdCap loop, slop theme.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8896 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 8896 --strictPort",
    "start": "vite preview --host 0.0.0.0 --port 8896 --strictPort",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "playwright": "^1.62.1",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
